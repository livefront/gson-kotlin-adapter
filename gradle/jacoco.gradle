apply plugin: 'jacoco'

jacocoTestReport {
    dependsOn(test)
    reports {
        xml.enabled true
        csv.enabled true
    }
}

jacoco {
    toolVersion = "0.8.7"
}

test {
    jacoco.excludes = ['jdk.internal.*']
    jacoco.includeNoLocationClasses = true
}
