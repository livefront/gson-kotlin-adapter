apply plugin: 'jacoco'

jacocoTestReport {
    dependsOn(test)
    reports {
        xml.enabled true
        csv.enabled true
    }
}

jacoco {
    toolVersion = "0.8.5"
}

test {
    jacoco.includeNoLocationClasses = true
}
